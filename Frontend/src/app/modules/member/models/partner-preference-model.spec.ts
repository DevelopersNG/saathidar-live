import { PartnerPreferenceModel } from './partner-preference-model';

describe('PartnerPreferenceModel', () => {
  it('should create an instance', () => {
    expect(new PartnerPreferenceModel()).toBeTruthy();
  });
});
